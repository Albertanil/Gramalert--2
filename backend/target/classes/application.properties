# Server Configuration
server.port=8080
spring.application.name=gramalert-plus

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/gramalert_db
spring.datasource.username=postgres
spring.datasource.password=91591580
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=BB4Fhni9vOdlYQ3NVmUEjR8GaxMXAkE95fMiYjQfCvbOv59jMJC

# Logging
logging.level.com.gramalertplus=INFO

# File Upload Configuration
file.upload-dir=./uploads

# Increase the maximum file sizes for uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Explicitly set the PostgreSQL dialect to prevent connection issues with DevTools
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

